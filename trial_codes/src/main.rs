fn main() {
        for number in (1..4).rev() { //(1..4) will range from [1,4) and rev() is a  method to reverse the range
            println!("{number}!");
        }
        println!("LIFTOFF!!!");
}